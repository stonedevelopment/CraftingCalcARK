package arc.resource.calculator.ui.search;

import androidx.fragment.app.Fragment;

import arc.resource.calculator.model.ui.InteractiveLayoutManager;
import arc.resource.calculator.model.ui.InteractiveViewModel;

public class SearchLayoutManager extends InteractiveLayoutManager {
    public SearchLayoutManager(Fragment fragment, InteractiveViewModel viewModel) {
        super(fragment, viewModel);
    }
}
