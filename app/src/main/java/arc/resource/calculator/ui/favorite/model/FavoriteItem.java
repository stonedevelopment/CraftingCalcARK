package arc.resource.calculator.ui.favorite.model;

import arc.resource.calculator.model.ui.interactive.InteractiveItem;

public class FavoriteItem extends InteractiveItem {
    public FavoriteItem(String uuid, String title, int viewType, String gameId) {
        super(uuid, title, viewType, gameId);
    }
}