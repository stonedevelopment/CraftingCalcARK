package arc.resource.calculator.ui.favorites.model;

import arc.resource.calculator.model.ui.interactive.InteractiveItem;

public class FavoritesItem extends InteractiveItem {
    public FavoritesItem(String uuid, String title, int viewType, String gameId) {
        super(uuid, title, viewType, gameId);
    }
}