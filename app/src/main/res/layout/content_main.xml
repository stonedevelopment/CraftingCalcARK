<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              xmlns:tools="http://schemas.android.com/tools"
              android:id="@+id/content_main"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              app:layout_behavior="@string/appbar_scrolling_view_behavior"
              tools:context=".MainActivity">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="@integer/weight_top_container"
        android:background="@color/colorPrimaryDark">

        <include
            layout="@layout/content_main_top"/>

    </FrameLayout>

    <FrameLayout
        android:id="@+id/content_main_bottom_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="@integer/weight_bottom_container"
        android:padding="@dimen/display_case_container_layout_padding">

        <include
            layout="@layout/content_main_bottom"/>

    </FrameLayout>

</LinearLayout>

