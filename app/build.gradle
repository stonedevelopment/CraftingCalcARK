apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'ARK_CC_KEY'
            keyPassword 'Gyu34dss'
            storeFile file('C:/Users/jared/Android/keystores/ark_cc_keystore.jks')
            storePassword 'Gyu34dss'
        }
    }
    compileSdkVersion 25
    buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId 'arc.resource.calculator'
        minSdkVersion 19
        targetSdkVersion 25
        versionCode 35
        versionName '1.1.0'
        signingConfig signingConfigs.config
        vectorDrawables.useSupportLibrary = true
    }
    buildTypes {
        release {
        }
        debug {
            debuggable true
        }
    }
    productFlavors {
        paid {
            applicationIdSuffix '.paid'
            buildConfigField 'boolean', 'PAID', "true"
        }
        free {
            buildConfigField 'boolean', 'PAID', "false"
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.0.0'
    compile 'com.android.support:design:25.0.0'
    compile 'com.android.support:support-v4:25.0.0'
    compile 'com.google.code.gson:gson:2.4'
    compile 'com.android.support:support-vector-drawable:25.0.0'
    compile 'com.google.firebase:firebase-core:9.8.0'
    compile 'com.google.firebase:firebase-crash:9.8.0'
    freeCompile 'com.google.firebase:firebase-ads:9.8.0'
    testCompile 'junit:junit:4.12'
}

apply plugin: 'com.google.gms.google-services'